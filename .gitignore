# Google Docs 連携ツールの中間生成物（生ダンプ・リビジョンID等。プロジェクト資産ではない）
bridge/

# Google ドライブへのポインタ（実体はクラウド側にあり、リポジトリ上では機能しない）
*.gdoc
*.gsheet
*.gslides

# Local dependencies and build output
node_modules/
dist/
.wrangler/

# Local secrets
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# OS / エディタ
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/

# Local-only working notes; do not publish to the remote repository
local/
